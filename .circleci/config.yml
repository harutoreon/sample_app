version: 2.1

jobs:
  spec:
    docker:
      - image: cimg/ruby:3.1.0
    steps:
      - checkout
      - run:
          command: bundle install
      - run:
          command: bundle exec rspec

workflows:
  spec-workflow:
    jobs:
      - spec
